server:
  tomcat:
    uri-encoding: UTF-8
    max-swallow-size: -1

spring:
  application:
    name: gateway
  profiles:
    active: dev

logging:
  level:
    root: info

#blackIp:
#  continueCounts: 3    # ip连续请求的次数
#  timeIntervalSec: 20  # ip判断的时间间隔,单位：秒
#  limitTimeSec: 30     # 黑名单ip限制的时间, 单位：秒

